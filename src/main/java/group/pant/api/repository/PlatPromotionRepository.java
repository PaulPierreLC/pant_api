package group.pant.api.repository;

import group.pant.api.model.PlatPromotion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlatPromotionRepository extends JpaRepository<PlatPromotion, Integer> {
}