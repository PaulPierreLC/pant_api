package group.pant.api.repository;

import group.pant.api.model.PlatRegime;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlatRegimeRepository extends JpaRepository<PlatRegime, Integer> {
}